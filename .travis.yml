language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gcloud-fullstack' ]; then cd .. && eval "mv $currentfolder generator-gcloud-fullstack" && cd generator-gcloud-fullstack; fi

